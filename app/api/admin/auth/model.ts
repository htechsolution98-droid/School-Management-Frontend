export interface AdminCredentials {
  username: string;
  password: string;
}

// Manually set credentials as requested
export const staticAdminUser = {
  username: "admin",
  password: "admin123",
};
